version: "2"
services:
  web:
    image: ${CI_REGISTRY_IMAGE}:${DOCKER_TAG}
    mem_limit: ${MEM}
    ports: ["80"]
    environment:
      - AWS_REGION
      - AWS_SECRET_NAME
      - NODE_ENV
      - CDN_PATH
      - API_URL
      - DOCKER_TAG
      - ENV
      - BASE_PATH
